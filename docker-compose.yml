services:
  api:
    build: .
    container_name: pbl-smarthub-api
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - ./app:/code/app              # <-- tambahan ini, WAJIB untuk auto-reload
      - ./data:/code/data
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    restart: unless-stopped